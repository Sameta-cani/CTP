# h 입력받기
h = int(input())

count = 0
for i in range(h + 1):
    for j in range(60):
        for k in range(60):
            # 시, 분, 초를 문자열로 합쳐서 '3'이 포함되어 있는지 확인
            if '3' in f"{i}{j}{k}":
                count += 1

print(count)
